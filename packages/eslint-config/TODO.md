# TODO

## Ideas

- [ ] Explore a comment-preserving updater for `eslint.config.mjs`, similar in spirit to `ensure-oxlintrc.mjs` but for JavaScript config files. Prefer a narrow text edit if the change stays simple; consider an AST-based codemod only if structural edits become necessary.
