$schema: "https://moonrepo.dev/schemas/project.json"

toolchains:
  typescript:
    enabled: true

tags:
  - "gh-pkg-npm"
  - "astro-plugin"

project:
  metadata:
    raa:
      run: publish
      rm: unpublish
